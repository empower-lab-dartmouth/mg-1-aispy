import { currentimage, game, lastmodel, object, colorObject, oldObject, readtext } from "./atoms";

export {
    currentimage,
    game,
    object,
    colorObject,
    oldObject,
    readtext,
    lastmodel,
}
